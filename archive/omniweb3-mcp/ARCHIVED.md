# omniweb3-mcp (Archived)

This folder was moved to `archive/omniweb3-mcp/` to preserve the original implementation.

Active development continues in:

- `web3mcp/` (Rust multi-chain MCP server)

Notes:
- This is a local archive within the repo (not a GitHub archive flag).
- Keep for reference / porting ideas, but avoid adding new features here.
