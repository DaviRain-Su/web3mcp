# Story: v0.3.1 Base Enhancements (Solana + EVM)

> 补齐 Solana 与 EVM 的基础只读能力：账户、交易、区块、Token 余额等。

## Goals
- 补齐 Solana 基础 RPC 能力（账户/交易/Token）
- 补齐 EVM 基础 RPC 能力（交易/区块/Nonce/Gas/Token）
- 提供只读工具优先（更安全、可扩展）

## Acceptance Criteria

### Solana Tools
- [x] `tools/account_info.zig` 获取账户信息
- [x] `tools/signature_status.zig` 查询交易状态
- [x] `tools/transaction.zig` 查询交易详情
- [x] `tools/token_balance.zig` SPL Token 余额
- [x] `tools/token_accounts.zig` 查询 Token Accounts
- [x] `tools/token_balances.zig` 查询钱包 Token 余额
- [x] `tools/request_airdrop.zig` 请求空投
- [x] `tools/tps.zig` 获取 TPS
- [x] `tools/slot.zig` 获取 slot
- [x] `tools/block_height.zig` 获取 block height
- [x] `tools/block.zig` 查询区块信息（slot）
- [x] `tools/registry.zig` 注册新增 Solana 工具

### EVM Tools (Unified Names)
- [x] `tools/block_number.zig` 获取最新区块高度
- [x] `tools/block.zig` 查询区块信息
- [x] `tools/transaction.zig` 查询交易详情
- [x] `tools/receipt.zig` 查询交易回执
- [x] `tools/nonce.zig` 获取地址 nonce
- [x] `tools/gas_price.zig` 查询 gas price
- [x] `tools/estimate_gas.zig` 估算 gas
- [x] `tools/call.zig` 只读合约调用 (eth_call)
- [x] `tools/token_balance.zig` ERC20 余额
- [x] `tools/registry.zig` 注册新增 EVM 工具

### Multi-Chain Config
- [x] Solana: devnet/testnet/mainnet/localhost
- [x] EVM: Ethereum/Avalanche/BNB
- [x] RPC endpoint override 参数支持

### Documentation
- [x] README.zig-0.16.md 更新工具列表
- [x] CHANGELOG.md 添加 v0.3.1 session

### Testing
- [ ] Solana: account_info / transaction / token_balance 本地或 devnet 验证
- [ ] EVM: block_number / gas_price / nonce / token_balance Anvil 验证

## Completion Status
- Start date: 2026-01-23
- Completion date: -
- Status: 🔨 In Progress
