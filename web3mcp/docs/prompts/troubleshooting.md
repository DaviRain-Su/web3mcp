# Prompt Pack — Troubleshooting

- "Run `system_healthcheck` and show me the JSON output."
- "If still broken, run `system_debug_bundle out_path=./debug_bundle.json` and show me the JSON output (no secrets)."
